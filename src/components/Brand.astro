<a class="brand" href="/" aria-label="Talon Expeditions home">
  <span class="brand-mark" aria-hidden="true"><span></span></span>
  <span class="brand-copy">
    <strong>Talon</strong>
    <small>Expeditions</small>
  </span>
</a>

<style>
  .brand { display:inline-flex; align-items:center; gap:.68rem; color:inherit; text-decoration:none; }
  .brand-mark { width:35px; height:35px; display:grid; place-items:center; position:relative; }
  .brand-mark::before,.brand-mark::after,.brand-mark span { content:""; position:absolute; display:block; }
  .brand-mark::before { width:28px; height:24px; bottom:3px; left:3px; background:var(--brand); clip-path:polygon(50% 0,100% 100%,0 100%); }
  .brand-mark::after { width:20px; height:17px; bottom:3px; left:7px; background:var(--page); clip-path:polygon(50% 0,100% 100%,0 100%); }
  .brand-mark span { width:9px; height:12px; bottom:3px; left:13px; background:var(--brand); clip-path:polygon(50% 0,100% 100%,0 100%); z-index:2; }
  .brand-copy { display:flex; flex-direction:column; line-height:.88; letter-spacing:-.02em; }
  .brand-copy strong { font-size:1rem; font-weight:750; }
  .brand-copy small { margin-top:.26rem; font-size:.66rem; text-transform:uppercase; letter-spacing:.18em; font-weight:700; opacity:.72; }
</style>
